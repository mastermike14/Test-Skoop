<%= f.hidden_field :id, :value => @group.id %>
<%= f.hidden_field :group, :value => @group.name %>
<%= f.label :message, "Message:" %><br />
<%= f.text_area :body, :id => "txtdesc", :name => "txtdesc", :cols => 60, :rows => 5 %><br />
<%= f.submit "Submit" %>
